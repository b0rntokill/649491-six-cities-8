export enum AppRoute {
  Login = '/login',
  Main = '/',
  Favorites = '/favorites',
  Place = '/offer/:id',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
